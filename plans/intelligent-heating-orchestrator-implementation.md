# Intelligent heating orchestrator – implementation record

Status: the first .NET 10 release is deployed in the existing Dockhand `daikin` stack as revision `233afa4` (2026-08-30). Production remains `ControlMode=Legacy` / `DhwWriter=Legacy`; LWT/FullActive are disabled. Health, anonymous access protection and a real accepted legacy ONECTA write are verified in [the production record](2026-08-30-production-verification.md). Intelligent control is not operationally approved.

Post-deployment source-only work is recorded in [session recovery and proxy regressions](2026-08-30-session-recovery-regressions.md), [account HTTP and logout verification](2026-08-31-account-http-verification.md), [room-quality verification](2026-08-31-room-quality-verification.md), [overall quality/API-contract verification](2026-08-31-status-quality-verification.md), [admin/account-deletion safety](2026-08-31-admin-account-safety-verification.md), and [HA catalog/sensor-picker verification](2026-08-31-ha-catalog-verification.md). Those changes have not been deployed. The ordinary legacy run on 2026-08-31 at 01:35 CEST is also verified read-only with an accepted ONECTA write; the older separate failing record remains unchanged. The subsequent 06:00 CEST read-only check confirmed unchanged health, images, account protection and Legacy/Legacy.

## Delivered

- Additive EF Core storage for site/room/entity configuration, five-minute telemetry, hourly aggregates, model versions, plans/steps, DHW cycles, control state, writer leases, command audit and fallback events.
- Account-owned Home Assistant connections with separate telemetry and control identities. REST/history/WebSocket telemetry maintains an account-scoped timestamped cache; the control client exposes only `number.set_value` for the account's exact `number.*` entity.
- HA URL, enablement, staleness limit, allowlisted entity and AES-256-GCM-encrypted tokens are stored per server-verified Daikin account. No global HA environment-variable or Docker-secret fallback remains, and token values, fragments and lengths are never returned or logged.
- Daikin writes resolve only the encrypted token belonging to the server-verified account and fail closed when it is missing. The global Daikin access-token setting remains isolated to accountless legacy/test compatibility and cannot be selected for an authenticated account.
- Five-minute HA history import/resampling that never overwrites live snapshots, plus WebSocket reconnection/backoff and a fresh REST snapshot after reconnect.
- Sensor unit normalization, bounds/rate/staleness checks, three-invalid exclusion, three-valid recovery, critical-room fallback and deduplicated room/hydraulic diagnostics.
- Forecast-aware 2R2C grey-box and COP training with held-out validation, heat-output/COP guards, nightly versioning, model-drift warnings, room-specific offset/inertia/disturbance profiles and 400-day raw retention. Synthetic critical-room fallback values protect control but are excluded from readiness and model training.
- Five-minute, duration-based DHW planner with ten-minute ONECTA starts, whole-cycle cost, empirical Eco/Comfort profiles, 90th-percentile reservation, 20-minute lock, lifecycle tracking, actual cost and two-sample 60 °C verification. Incomplete price coverage selects the earliest safe start with a conservative price instead of risking availability, and the cost includes the estimated space-heating comfort margin consumed by the cycle.
- EMHASS 15-minute/48-hour space-heating coordination with locked DHW capacity and a 45-second hard timeout. The official container's documented `opt_res_latest.csv` is shared read-only, freshness/columns/horizon are validated, and mutual-exclusion groups prevent simultaneous space heating and locked DHW use of compressor capacity. EMHASS receives runtime data only and cannot access HA.
- PI-based LWT deviation controller with anti-windup, rate/change limits, DHW/defrost freeze, latched write failures and audited fail-to-zero attempts. A P1P2 write is accepted only after a fresh matching Home Assistant state confirms the requested value.
- Sequential `Legacy` → `Shadow` → `LwtActive` → `FullActive` mode service, measured readiness gates, installation ownership, expiring database-backed DHW write exclusion, atomic writer handover and visible rollback.
- A signed, database-backed session reuses the server-verified Daikin identity for legacy and thermal functionality. All account APIs require that session, all authenticated mutations require antiforgery validation, and administrative operations still require the admin role.
- The login gate offers an accessible, read-only retry after a failed session check. It hides cached installation views while verification is failing and never renders raw proxy/identity-provider error responses. This follow-up is locally verified, not yet deployed.
- Shared production/test HTTP registrations verify signed sessions, revocation, account/API isolation, CSRF and admin authorization without starting control workers. Account APIs return explicit 401/403 instead of cookie-login redirects; admin discovery includes credentialless existing records without modifying them. These fixes are locally verified, not yet deployed.
- Failed logout is clearly reported without revealing raw server errors or falsely claiming success. Responsive navigation has valid list markup, distinct landmark names and non-overlapping mobile links; logout/retry and keyboard navigation are covered locally, not yet deployed.
- Room cards use actual sensor quality/exclusion metadata and distinguish current measurements, stored fallback values, stale/imported data and fetch failures. Only current valid readings get a comfort margin; valid cold rooms retain their warning. Dated event history preserves Information/Warning/ActionRequired without presenting old events as active alerts. These UI changes are locally verified, not deployed.
- Overall status now assesses the signed-in account's enabled sources, exclusion/recovery, numeric/boolean values, quality metadata, source, snapshot age and configuration freshness. It returns an additive explanation and cannot infer Valid from a critical-room fallback. The UI explains status, hides cached claims on fetch errors and ages green badges independently of polling. Actual numeric API enums are translated in the browser; mode requests retain the existing numeric JSON contract and all activation gates. These fixes are locally verified, not deployed, and do not modify any controller.
- Admin account deletion now fails closed with an explicit HTTP 409 before any token/role removal. The old handler falsely reported complete deletion while leaving authenticated sessions, HA access, auto-apply and thermal writers intact. The admin UI explains the block, never issues deletion requests, hides cached account data on failed permission/list checks and provides accessible read-only recovery. This source-only safety guard is not a complete deletion lifecycle and is not deployed; active-writer coordination and audit-retention policy must be defined before deletion can safely return.
- The account-scoped HA catalog now distinguishes unavailable/invalid/stale values, disconnected or superseded snapshots, and preliminary role/unit compatibility without touching sensor-health counters. Shared room/entity pickers preserve saved IDs through missing entities and fetch failures, explain compatibility and both timestamps, and expire old local approvals. HTTP, numeric-contract, keyboard, accessibility and 320-pixel flows are locally verified, not deployed. This is not collector-health or readiness approval; connection revision reload and collector edge-case tests remain open.
- Swedish responsive UI for Overview, Plan, Rooms, Hot water, Model, Events and Settings while keeping the legacy grid/admin pages.
- Vitest/React Testing Library/accessibility tests and Playwright critical-flow coverage in the PR gate.
- Digest-locked EMHASS reference topology in `docker-compose.thermal.example.yml` with a private internal network and persistent `/share` and `/data`.

## Verified locally

- Existing baseline before implementation: 425 backend tests passed, 9 skipped.
- Latest follow-up on 2026-08-31: a new .NET 10 Release build and full backend suite passed **753 tests, with 6 existing skips and 0 failures**. HA catalog quality/account-isolation added 65 cases to the preceding 688/6 account-deletion baseline. Account-deletion safety had added 20 real HTTP cases and replaced 3 older non-HTTP placeholders, on top of 671/6. The deployed PR #120 passed 521/9; none of the follow-up changes are deployed. Local SDK 10.0.111 was invoked explicitly; repo/CI/Docker remain pinned to 10.0.400.
- Production forwarding and antiforgery registrations are tested together using the real middleware and ephemeral test keys: trusted IPv4/mapped IPv6, untrusted headers, single-hop processing, ignored host spoofing, Secure/HttpOnly cookies, and rejection of missing/invalid/cross-account CSRF tokens. These tests do not start the full application or replace authenticated production acceptance.
- An isolated TestServer uses the real account session/admin handlers, cookie validation, API/CSRF boundary and rate limiter. It verifies logout revocation/replay, stale/revoked/forged identity rejection, every mutation method and administrator access. EF InMemory and synthetic identities do not prove live PostgreSQL behavior or Daikin OAuth login; no control services or production migrations are started.
- Focused thermal unit/integration-contract suite: 75 passed and 0 failed, including 92/96/100-quarter DST days, every allowed ten-minute DHW start boundary, incomplete-price fallback, empirical backup-heater phases, delayed/missed DHW runs, EMHASS CSV validation/mutual exclusion/hard timeout/tariff behavior, confirmed P1P2 writes and every LWT safe-zero trigger.
- Frontend: **138** Vitest component, interaction, accessibility and presentation tests passed in the latest follow-up. These include catalog unit/age checks, persisted selections through errors, supported temperature/power conversion, additive HTTP-contract preservation, account-deletion blocking, safe admin/list-fetch recovery, room-quality/comfort/history, numeric API enum translation/request encoding and status/error/age recovery. JSDOM accessibility checks still exclude color contrast; full assistive-technology/contrast validation remains separate.
- Playwright: **18 applicable desktop/mobile flows** passed locally (6 existing duplicate-project exclusions), including login/session/logout, readiness blocking, dirty-state preview, history import, room/status quality and fetch failures, admin deletion/permission/list-error handling, and the new read-only catalog selection/error/recovery flow. Mobile overflow and keyboard navigation are checked down to 320 px. Numeric enum fixtures match the actual HTTP contract. Screenshots of status, rooms, admin, sensor fields and errors are inspected locally; they are not production acceptance.
- TypeScript/Vite production build passed in the latest follow-up.

Earlier verification, retained for traceability (not all checks were rerun in the follow-up):

- `npm audit` reported zero known vulnerabilities after upgrading React Router and Vite to compatible fixed releases.
- Release `dotnet publish` produced a `net10.0` artifact successfully. The solution, Dockerfile and CI are all on .NET 10; the Release build completed with zero warnings and errors.
- NuGet's current advisory scan reported no vulnerable direct or transitive packages.
- All five generated thermal/account migrations are additive: the first creates the isolated thermal tables/indexes and the later migrations only extend thermal or credential/account storage. `dotnet ef migrations has-pending-model-changes --no-build` and the model-drift regression test report no pending model changes.
- Docker Compose configuration validation passed.
- EMHASS `v0.18.0` tag and multi-arch digest were verified against GHCR. Local Docker Desktop was unavailable at that stage. The existing production stack now includes the running, port-private EMHASS container, but `Emhass:Enabled=false`; live optimizer/house-model acceptance remains pending.

## Operational acceptance still required

Code verification does not satisfy the real-house gates. The initial deployment is verified in `Legacy` with existing ONECTA settings preserved. The user still needs to complete the authenticated account UI check and configure the account-owned HA connection before collection/model validation can begin. Continue read-only observation of ordinary legacy runs, then collect/validate the required shadow period, weather-curve days, model errors, DHW cycles and hygiene cycle through the UI. `FullActive` remains blocked until readiness is green.

The timestamp-only `overallDataQuality` defect is corrected and covered by HTTP/UI regressions. The old admin deletion defect is now reproduced and safely blocked in local code; do not use the still-deployed old deletion endpoint. A complete safe account-deletion lifecycle remains open, not silently substituted with partial deletion. Review the accumulated source-only series and run applicable CI before any justified update of the existing Dockhand stack. Do not redeploy or trigger another Daikin test write just because the heartbeat runs. No live account deletion or changes to credentials/settings are authorized by this follow-up. Six pre-existing backend test exclusions and real authenticated production acceptance remain open; the isolated HTTP test host is not full application-startup coverage.

The bounded entity-picker follow-up is complete in local code. Next, safely reload each account's HA WebSocket subscription when its connection revision changes; the existing manager only compares enabled account IDs. The catalog now rejects a superseded snapshot, but the GET refresh button deliberately does not reconnect the socket. Add isolated reconnect/cancellation/race tests and clear UI feedback without real credential changes. Separately cover non-finite values, malformed attributes and missing/future raw timestamps through collector/history/readiness; preliminary catalog checks do not replace those controls or prove active-control safety.

Actual container/network/rollback facts are recorded in the production record. Windows Controlled Folder Access blocks changes to the shared infrastructure project's documents. A prepared patch is saved as `plans/2026-08-30-shared-infrastructure-update.patch`; the shared files remain unchanged and the protection must not be disabled or bypassed.
