import { Typography } from '@mui/material';

export default function DashboardPage() {
  return (
    <div>
      <Typography variant="h4" gutterBottom>
        Dashboard
      </Typography>
      <Typography>
        Phase 6 will implement: Auth, Price Chart, Schedule Grid, History
      </Typography>
    </div>
  );
}
