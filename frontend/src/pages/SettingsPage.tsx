import { Typography } from '@mui/material';

export default function SettingsPage() {
  return (
    <div>
      <Typography variant="h4" gutterBottom>
        Settings
      </Typography>
      <Typography>
        Phase 8 will implement: User settings, Zone selection, Token revocation
      </Typography>
    </div>
  );
}
